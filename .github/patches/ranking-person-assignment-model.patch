diff --git a/app/src/main/java/com/example/data/model/QuestionInteraction.kt b/app/src/main/java/com/example/data/model/QuestionInteraction.kt
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/app/src/main/java/com/example/data/model/QuestionInteraction.kt
@@ -0,0 +1,100 @@
+package com.example.data.model
+
+enum class QuestionInteractionKind {
+    STANDARD,
+    RANK_ORDER,
+    PERSON_ASSIGNMENT
+}
+
+enum class PersonSide(val token: String) {
+    USER("user"),
+    PARTNER("partner");
+
+    companion object {
+        fun fromToken(token: String): PersonSide? = entries.firstOrNull { it.token == token }
+    }
+}
+
+object QuestionInteractionPolicy {
+    fun resolve(pack: QuestionPack, question: Question): QuestionInteractionKind {
+        when (question.interaction?.trim()?.lowercase()) {
+            "person_assignment" -> return QuestionInteractionKind.PERSON_ASSIGNMENT
+            "rank_order" -> return QuestionInteractionKind.RANK_ORDER
+            "standard" -> return QuestionInteractionKind.STANDARD
+        }
+
+        return if (pack.cat == "h360_ranking" || pack.tags.any { it == "mechanik_ranking" }) {
+            QuestionInteractionKind.RANK_ORDER
+        } else {
+            QuestionInteractionKind.STANDARD
+        }
+    }
+}
+
+object PersonAssignmentCodec {
+    private const val PREFIX = "person-assignment-v1:"
+    private const val ITEM_SEPARATOR = "\u001F"
+    private const val VALUE_SEPARATOR = "\u001E"
+
+    private fun clean(value: String): String = value
+        .replace(ITEM_SEPARATOR, " ")
+        .replace(VALUE_SEPARATOR, " ")
+
+    fun encode(options: List<String>, assignments: Map<String, PersonSide>): String {
+        require(options.distinct().size == options.size) { "Person-assignment options must be unique" }
+        require(options.all { assignments.containsKey(it) }) { "Every role must be assigned" }
+        return PREFIX + options.joinToString(ITEM_SEPARATOR) { option ->
+            clean(option) + VALUE_SEPARATOR + assignments.getValue(option).token
+        }
+    }
+
+    fun decode(value: String, options: List<String>): LinkedHashMap<String, PersonSide>? {
+        if (!value.startsWith(PREFIX) || options.distinct().size != options.size) return null
+        val payload = value.removePrefix(PREFIX)
+        if (payload.isBlank() && options.isNotEmpty()) return null
+
+        val decoded = linkedMapOf<String, PersonSide>()
+        val records = if (payload.isBlank()) emptyList() else payload.split(ITEM_SEPARATOR)
+        for (record in records) {
+            val parts = record.split(VALUE_SEPARATOR, limit = 2)
+            if (parts.size != 2 || decoded.containsKey(parts[0])) return null
+            val side = PersonSide.fromToken(parts[1]) ?: return null
+            decoded[parts[0]] = side
+        }
+
+        if (decoded.keys.toList() != options) return null
+        return LinkedHashMap(decoded)
+    }
+}
+
+object RankingAnswerCodec {
+    private const val PREFIX = "ranking-v1:"
+    private const val SEPARATOR = "\u001F"
+
+    private fun clean(value: String): String = value.replace(SEPARATOR, " ")
+
+    fun encode(order: List<String>): String = PREFIX + order.joinToString(SEPARATOR) { clean(it) }
+
+    fun decode(value: String, options: List<String>): List<String>? {
+        if (!value.startsWith(PREFIX) || options.distinct().size != options.size) return null
+        val payload = value.removePrefix(PREFIX)
+        val order = if (payload.isBlank()) emptyList() else payload.split(SEPARATOR)
+        if (order.size != options.size) return null
+        if (order.distinct().size != order.size) return null
+        if (order.toSet() != options.toSet()) return null
+        return order
+    }
+}
+diff --git a/app/src/main/java/com/example/data/model/Models.kt b/app/src/main/java/com/example/data/model/Models.kt
+index 0551b1f..2222222 100644
+--- a/app/src/main/java/com/example/data/model/Models.kt
++++ b/app/src/main/java/com/example/data/model/Models.kt
+@@ -81,7 +81,8 @@ data class Topic(
+ data class Question(
+     val q: String,
+     val options: List<String> = emptyList(),
+-    val defaultMine: String? = null
++    val defaultMine: String? = null,
++    val interaction: String? = null
+ )
+ 
+ data class QuestionPack(
+diff --git a/app/src/main/java/com/example/data/DevGenTypes.kt b/app/src/main/java/com/example/data/DevGenTypes.kt
+index f075b65..3333333 100644
+--- a/app/src/main/java/com/example/data/DevGenTypes.kt
++++ b/app/src/main/java/com/example/data/DevGenTypes.kt
+@@ -19,7 +19,8 @@ data class GenCategory(
+ data class GenQuestion(
+     val q: String,
+     val options: List<String> = emptyList(),
+-    val defaultMine: String? = null
++    val defaultMine: String? = null,
++    val interaction: String? = null
+ )
+ 
+ data class GenPack(
+diff --git a/app/src/main/java/com/example/data/DeveloperDataManager.kt b/app/src/main/java/com/example/data/DeveloperDataManager.kt
+index 19d23e1..4444444 100644
+--- a/app/src/main/java/com/example/data/DeveloperDataManager.kt
++++ b/app/src/main/java/com/example/data/DeveloperDataManager.kt
+@@ -102,7 +102,12 @@ object DeveloperDataManager {
+                     topic = gp.topic,
+                     type = gp.type,
+                     questions = gp.questions.map {
+-                        Question(q = it.q, options = it.options, defaultMine = it.defaultMine)
++                        Question(
++                            q = it.q,
++                            options = it.options,
++                            defaultMine = it.defaultMine,
++                            interaction = it.interaction
++                        )
+                     },
+                     pairs = gp.pairs,
+                     emoji = gp.emoji
+@@ -239,7 +244,8 @@ object DeveloperDataManager {
+                     Question(
+                         q = qObj.getString("q"),
+                         options = opts,
+-                        defaultMine = if (qObj.has("defaultMine")) qObj.getString("defaultMine") else null
++                        defaultMine = if (qObj.has("defaultMine")) qObj.getString("defaultMine") else null,
++                        interaction = qObj.optString("interaction", "").takeIf { it.isNotBlank() }
+                     )
+                 )
+             }
+@@ -298,6 +304,7 @@ object DeveloperDataManager {
+             q.options.forEach { oArr.put(it) }
+             qObj.put("options", oArr)
+             q.defaultMine?.let { qObj.put("defaultMine", it) }
++            q.interaction?.takeIf { it.isNotBlank() }?.let { qObj.put("interaction", it) }
+             qArr.put(qObj)
+         }
+         obj.put("questions", qArr)
+diff --git a/app/src/main/java/com/example/data/DevExporter.kt b/app/src/main/java/com/example/data/DevExporter.kt
+index ea90877..5555555 100644
+--- a/app/src/main/java/com/example/data/DevExporter.kt
++++ b/app/src/main/java/com/example/data/DevExporter.kt
+@@ -257,7 +257,9 @@ object DevExporter {
+                             .append(q.options.joinToString(", ") { str(it) })
+                             .append("), defaultMine = ")
+                             .append(q.defaultMine?.let { str(it) } ?: "null")
++                            .append(", interaction = ")
++                            .append(q.interaction?.let { str(it) } ?: "null")
+                             .append(")")
+                         sb.append(if (questionIndex == p.questions.lastIndex) "\n" else ",\n")
+                     }
+diff --git a/app/src/main/java/com/example/data/GeneratedHarmonyAdrenaline360Section20TeamworkChallenge.kt b/app/src/main/java/com/example/data/GeneratedHarmonyAdrenaline360Section20TeamworkChallenge.kt
+index fa3f536..6666666 100644
+--- a/app/src/main/java/com/example/data/GeneratedHarmonyAdrenaline360Section20TeamworkChallenge.kt
++++ b/app/src/main/java/com/example/data/GeneratedHarmonyAdrenaline360Section20TeamworkChallenge.kt
+@@ -68,7 +68,7 @@ object GeneratedHarmonyAdrenaline360Section20TeamworkChallenge {
+             emoji = "🤝",
+             questions = listOf(
+                 GenQuestion(q = "Was ist das Wichtigste in einer funktionierenden Partnerschaft? Ordne: Vertrauen, Kommunikation, Humor, Leidenschaft", options = listOf("Vertrauen", "Kommunikation", "Humor", "Leidenschaft")),
+-                GenQuestion(q = "Wer übernimmt welche Rolle bei gemeinsamen Plänen? Rank: Visionär/Ideen, Detailplaner, Ausführer, Qualitätsprüfer", options = listOf("Visionär/Ideen", "Detailplaner", "Ausführer", "Qualitätsprüfer")),
++                GenQuestion(q = "Wer übernimmt welche Rolle bei gemeinsamen Plänen? Rank: Visionär/Ideen, Detailplaner, Ausführer, Qualitätsprüfer", options = listOf("Visionär/Ideen", "Detailplaner", "Ausführer", "Qualitätsprüfer"), interaction = "person_assignment"),
+                 GenQuestion(q = "Was hilft am besten bei akutem Stress? Ordne: Feste Umarmung, Praktische Hilfe, Raum zum Atmen, Humor & Ablenkung", options = listOf("Feste Umarmung", "Praktische Hilfe", "Raum zum Atmen", "Humor & Ablenkung")),
+                 GenQuestion(q = "Welcher Meilenstein schweißt am stärksten zusammen? Rankt: Erste gemeinsame Wohnung, Große Reise, Schwierige Zeit gemeistert, Lebensziel erreicht", options = listOf("Erste gemeinsame Wohnung", "Große Reise", "Schwierige Zeit gemeistert", "Lebensziel erreicht")),
+                 GenQuestion(q = "Wie trefft ihr die besten Entscheidungen? Ordne: Gemeinsam diskutieren, Bauchgefühl folgen, Pro & Contra Liste, Zeit verstreichen lassen", options = listOf("Gemeinsam diskutieren", "Bauchgefühl folgen", "Pro & Contra Liste", "Zeit verstreichen lassen")),
