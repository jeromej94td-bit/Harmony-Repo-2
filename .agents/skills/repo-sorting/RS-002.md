# RS-002 — funktionierende ZIP-Sortierung

Referenznummer: `RS-002`

Status: aktiv nach Synchronisierung mit `main`.

Quelle: vom Nutzer bereitgestellte funktionierende App-ZIP `harmony-2.0 (2).zip` vom 2026-08-30.

## Abgrenzung zu RS-001

`RS-001` bleibt der archivierte historische Sortierstand und darf nicht als aktuelle Quelle reaktiviert werden.

`RS-002` übernimmt den in der funktionierenden ZIP vorhandenen Topic-Router, ergänzt aber beim Port auf GitHub bewusst die seitdem bereits auf `main` hinzugekommenen Curation-Stufen statt ältere ZIP-Dateien blind über neuere Repository-Dateien zu kopieren.

## Aktiver Vertrag

- `Harmony360SectionTopicSorting` ist wieder der finale nicht-destruktive Topic-Pass für Harmony-360.
- Die in der ZIP definierten stabilen Pack-ID-Zuordnungen bleiben erhalten.
- Nicht explizit nach `moral` verschobene Packs mit Tag `h360_section_12_kommunikation_konflikte` werden auf `beziehung` normalisiert.
- `GeneratedContentRegistry` normalisiert GENERATED- und stale CUSTOM-Packs mit demselben Router.
- `HarmonyPacksData.PACKS` normalisiert beim finalen Zusammenführen auch Default-, Expansion- und dynamische Packs, damit spätere Dev-Studio-Daten die funktionierende Sortierung nicht wieder überschreiben.
- Die neueren Main-Curations, insbesondere `Harmony360MutprobeRedistributionCuration`, bleiben erhalten.
- Section-06-Curation bleibt bei 16 kuratierten Section-Packs; die Stage-05.1-Pipeline enthält zusätzlich `h360_need_now_quick` und hat deshalb 17 Packs.

Vor späteren Änderungen immer den dann aktuellen `main` und `.agents/skills/repo-sorting/SKILL.md` erneut prüfen.
