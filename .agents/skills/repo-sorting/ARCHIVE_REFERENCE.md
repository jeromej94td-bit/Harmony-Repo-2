# Repo-Sorting Archive References

- `RS-001` — ehemaliger finaler `Harmony360SectionTopicSorting`-Pass aus PR #141. Vollständiger Stand archiviert auf Branch `archive/RS-001-topic-sorting`.

`RS-001` ist **nicht** die aktuelle Source of Truth und darf nicht automatisch wieder aktiviert werden. Vor einer Reaktivierung zuerst den dann aktuellen `main` und die realen Section-/Curation-Pfade prüfen.
